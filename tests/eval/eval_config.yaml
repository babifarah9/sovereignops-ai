metrics_to_run:
  - sovereignops_compliance

custom_metrics:
  # Deterministic local grading avoids extra judge-model calls and credit usage.
  - name: sovereignops_compliance
    custom_function_file: sovereignops_compliance.py
